{
  "scripts": {
    "dev": "run-p dev:*",
    "dev:wrangler": "wrangler dev --compatibility-date=2023-01-01 dist/_worker.js --live-reload",
    "dev:esbuild": "esbuild --bundle src/index.ts --format=esm --watch --outfile=dist/_worker.js",
    "build": "esbuild --bundle src/index.ts --format=esm --outfile=dist/_worker.js",
    "deploy": "wrangler pages publish --project-name just-be-dev dist"
  },
  "dependencies": {
    "hono": "^3.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230628.0",
    "esbuild": "^0.19.2",
    "npm-run-all": "^4.1.5",
    "wrangler": "^3.6.0"
  }
}
